<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
      <%= link_to t('helpers.link.logo'), current_user.is_a?(Admin) ? admin_tests_path : root_path, class: 'navbar-brand' %> 

    <span class="navbar-text">
      <% unless current_user.is_a?(Admin) %>
        
        <% if user_signed_in? %>
          Welcome, <b><%= current_user.email %></b> <%= link_to t('.guru'), root_path %> |
          <%= link_to t('helpers.link.log_out'), destroy_user_session_path,  data: { turbo_method: :delete,  turbo_confirm: t('helpers.data.are_you_sure') }, class: 'btn btn-outline-danger btn-sm' %>

        <% else %>  
          <%= link_to t('helpers.link.sign_up'), new_user_registration_path,  class: 'btn btn-outline-primary btn-sm me-2' %>
          <%= link_to t('helpers.link.log_in'), new_user_session_path, class: 'btn btn-outline-success btn-sm' %>
        <% end %>
      <% end %>
    </span>
  </div>
</nav>
