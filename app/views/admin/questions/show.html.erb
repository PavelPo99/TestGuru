<h1><%= t('.header', test_title: @question.test.title )%></h1>

<p>
  <%= t('.question_body', body: @question.body) %>
</p>

<h3><%= t('.answer') %></h3>

<%= link_to t('.new_answer'), new_admin_question_answer_path(@question.id) %>


<ol>
  <%= render @question.answers %>
</ol>

<%= link_to t('helpers.link.back'), admin_test_path(@question.test) %>
