<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.category.title %></td>
  <td><%= test.author.first_name %></td>
  <td><%= test.questions.count %></td>

  <td><%= button_to 'Start', start_test_path(test) if test.questions.count > 0 %></td>

</tr>
