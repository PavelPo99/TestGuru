<p><%= t('.greeting', email: @resource.email) %></p>

<p><%= t('.request_change') %></p>

<p><%= link_to t('.change_button'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('.ignore_request') %></p>
<p><%= t('.no_change') %></p>
