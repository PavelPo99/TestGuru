<h1>Questions for <%= Test.find(params[:test_id]).title %></h1>

<p><%= link_to 'New Question', new_test_question_path %></p>

<table> 
  <tr>
    <th>Questions</th>
    <th>Answers</th>
    <th colspan='3'>Action</th>
  </tr>

  <%= render 'question' %>
</table>

<br><%= link_to 'Back', tests_path %>
